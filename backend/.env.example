# Healthcare Study Companion - Environment Variables Template
# Copy this file to local.env and fill in your actual values

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application environment (development, staging, production)
ENVIRONMENT=development

# Application name (optional, defaults to "Healthcare Study Companion API")
APP_NAME=Healthcare Study Companion API

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Primary database connection URL (TiDB Serverless recommended)
# Format: mysql+pymysql://username:password@host:port/database?ssl_ca=path&ssl_verify_cert=true&ssl_verify_identity=true
DATABASE_URL=mysql+pymysql://user:password@host:port/database

# Alternative: Individual database connection parameters
# (Only used if DATABASE_URL is not provided)
DATABASE_HOST=localhost
DATABASE_PORT=4000
DATABASE_USER=root
DATABASE_PASSWORD=
DATABASE_NAME=healthcare_study

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# JWT secret key for token signing (REQUIRED - change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated list or "*" for all)
# Production example: https://yourdomain.com,https://www.yourdomain.com
# Development: http://localhost:3000,http://127.0.0.1:3000
CORS_ORIGINS=*

# =============================================================================
# AI/LLM CONFIGURATION
# =============================================================================

# Primary LLM provider (openai, anthropic, huggingface, together, mistral)
LLM_PROVIDER=mistral

# Default models for text generation and embeddings
DEFAULT_LLM_MODEL=mistral-small
EMBEDDING_MODEL=mistral-embed
LLM_MODEL_ID=mistral-small
EMBEDDING_MODEL_ID=mistral-embed

# API Keys (provide keys for your chosen provider)
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
TOGETHER_API_KEY=your_together_api_key_here
MISTRAL_API_KEY=your_mistral_api_key_here

# LLM Generation Parameters
LLM_RATE_LIMIT_PER_MINUTE=60
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.7

# =============================================================================
# EMAIL CONFIGURATION (Optional)
# =============================================================================

# Enable email functionality (true/false)
EMAIL_ENABLED=false

# SMTP server configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# Railway deployment (if using Railway)
RAILWAY_DEPLOYMENT_ID=
RAILWAY_GIT_COMMIT_SHA=

# Auto-rollback on deployment failure (true/false)
AUTO_ROLLBACK_ON_FAILURE=true

# =============================================================================
# LOGGING AND MONITORING
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable structured logging (true/false)
STRUCTURED_LOGGING=true

# =============================================================================
# PERFORMANCE CONFIGURATION
# =============================================================================

# Database connection pool settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=30

# Async operation limits
MAX_CONCURRENT_AI_REQUESTS=10
MAX_CONCURRENT_DOCUMENT_PROCESSING=5

# File upload limits
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,txt,docx,md