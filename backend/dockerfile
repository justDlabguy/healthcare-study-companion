# Use a Python image that is based on Debian, which uses apt-get.
FROM python:3.11-slim

# Install system-level dependencies required for building mysqlclient
# This is the critical step to fix your build error.
RUN apt-get update && apt-get install -y default-libmysqlclient-dev gcc

# Set the working directory inside the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
# We do this first so Docker can cache the pip install layer
COPY requirements.txt ./

# Install all Python dependencies from requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of your application code into the container
COPY . .

# Expose the port that your FastAPI application will run on
EXPOSE 8000

# Define the command to start your application
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
